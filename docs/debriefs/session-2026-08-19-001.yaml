# ================================================================
# GENERIC SESSION DEBRIEF — SherpaCarta
# Generated by Standard Project Kit (skill standard-project-kit)
# ================================================================

runbook:
  name: Session Debrief
  version: "2.0"
  trigger:
    phrases: ["run session debrief"]

project_config:
  project_name: "SherpaCarta"
  context_file: null
  framework_terms: null
  id_prefixes:
    architectural_decision: "AD-"
    open_question: "OQ-"
  output_location: "docs/debriefs/"

output_schema:
  session_id: "session-2026-08-19-001"
  date: "2026-08-19"
  status: "confirmed"

  summary:
    purpose: "Trial the new Standard Project Kit on SherpaCarta and re-sync the THOR copy after a fresh git pull."
    outcomes:
      - "THOR copy /root/ref/sherpacarta pulled fresh from origin/main — now on d087062, 19 commits caught up (74 files changed: Nostr integration + 2-min HyperFrames film landed)."
      - "Adopted the senior-engineer Standard Project Kit as the default build/handoff methodology across all projects."
      - "Established /root/ref/sherpacarta as the live/deployable + handoff home on THOR."

  architectural_decisions:
    - id: "AD-001"
      decision: "Treat /root/ref/sherpacarta on THOR as the live/deployable + handoff home. M3 and M4 clones are working copies only, synced via git push/pull — never by copying folders between machines."
      rationale: "Keeps a single source of truth on the ops brain (THOR) while M3/M4 code locally via Grok→GitHub push. Avoids drift and duplicates; the memory rule already forbids copying working folders between machines."
      status: "agreed"

  open_questions:
    - id: "OQ-001"
      question: "When should the session debrief YAML be run for SherpaCarta — end of every working session, or only at milestones?"
      context: "The Standard Project Kit says 'end of any meaningful session'. SherpaCarta is largely M3/M4 code work, so THOR may not see every session."
      priority: "medium"
      resolution: "Run the debrief at the end of any meaningful THOR session (ops, deploy, handoff, or a non-trivial review), and any time the user says 'run session debrief'. M3/M4 code-only sessions arrive on THOR via git push; the THOR debrief then covers whatever landed. Do not run it for trivial pings or single-command fixes."
      status: "resolved"

    - id: "OQ-002"
      question: "Should the debrief files be committed to the repo (docs/debriefs/) or kept out of git?"
      context: "They are machine-readable records. Committing them keeps continuity for any clone; keeping them out avoids noise. AD-001 makes THOR the home either way."
      priority: "low"
      resolution: "Yes — commit debriefs to the repo under docs/debriefs/. They carry the AD-/OQ- ID continuity that any clone (M3/M4) needs, and they cost nothing to keep. They are documentation, not generated output."
      status: "resolved"

  session_notes:
    deprecated_items: []
    corrections_to_prior_sessions: []
    other:
      - "Trial run of the Standard Project Kit. Debrids were previously generated manually or not at all for SherpaCarta; this is the first machine-readable debrief."
